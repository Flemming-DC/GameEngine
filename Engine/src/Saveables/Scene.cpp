#include "Scene.h"
#include "ErrorChecker.h"

/*
Scene::Scene(std::string name) : name(name)
{
	//entities = MakeEntities();
	//LoadSpatialData(entities);
}*/
/*
std::vector<Entity>& Scene::MakeEntities()
{
	RaiseError("You are calling the base function of MakeEntities, but should call the virtual overrides.");
	std::vector<Entity> entitites = {};
	return entitites;
};*/

void Scene::LoadSpatialData(const std::vector<Entity>& entities)
{

}
